pipeline {
  agent {
    docker {
      image 'abhishekf5/maven-abhishek-docker-agent:v1'
      args '--user root -v /var/run/docker.sock:/var/run/docker.sock'
    }
  }
  
  stages {
    stage('Checkout') {
      steps {
        sh 'echo "Checkout stage passed"'
        // Uncomment when ready to use actual git repo
        // git branch: 'main', url: 'https://github.com/Olorunife/Jenkins-Zero-To-Hero-forked.git'
      }
    }
    
    stage('Build and Test') {
      steps {
        sh 'ls -ltr'
        dir('java-maven-sonar-argocd-helm-k8s/spring-boot-app') {
          sh 'mvn clean package'
        }
      }
      post {
        always {
          // Archive the built JAR
          archiveArtifacts artifacts: '**/target/*.jar', fingerprint: true
          // Publish test results
          publishTestResults testsFilePattern: '**/target/surefire-reports/TEST-*.xml'
        }
      }
    }
    
    stage('Static Code Analysis') {
      environment {
        SONAR_URL = "http://13.41.200.53:9000"
      }
      steps {
        withCredentials([string(credentialsId: 'sonarqube', variable: 'SONAR_AUTH_TOKEN')]) {
          dir('java-maven-sonar-argocd-helm-k8s/spring-boot-app') {
            sh 'mvn sonar:sonar -Dsonar.login=$SONAR_AUTH_TOKEN -Dsonar.host.url=${SONAR_URL}'
          }
        }
      }
    }
    
    stage('Build and Push Docker Image') {
      environment {
        DOCKER_IMAGE = "abhishekf5/ultimate-cicd:${BUILD_NUMBER}"
        REGISTRY_CREDENTIALS = credentials('docker-cred')
      }
      steps {
        script {
          dir('java-maven-sonar-argocd-helm-k8s/spring-boot-app') {
            // Build Docker image
            sh 'docker build -t ${DOCKER_IMAGE} .'
            
            // Push to registry
            docker.withRegistry('https://index.docker.io/v1/', "docker-cred") {
              def dockerImage = docker.image("${DOCKER_IMAGE}")
              dockerImage.push()
            }
          }
        }
      }
    }
    
    stage('Update Deployment File') {
      environment {
        GIT_REPO_NAME = "Jenkins-Zero-To-Hero-forked"
        GIT_USER_NAME = "Olorunife"
      }
      steps {
        withCredentials([string(credentialsId: 'github', variable: 'GITHUB_TOKEN')]) {
          sh '''
            git config user.email "ifeo.adesanya@gmail.com"
            git config user.name "IfeOluwa Adesanya"
            BUILD_NUMBER=${BUILD_NUMBER}
            sed -i "s/replaceImageTag/${BUILD_NUMBER}/g" java-maven-sonar-argocd-helm-k8s/spring-boot-app-manifests/deployment.yml
            git add java-maven-sonar-argocd-helm-k8s/spring-boot-app-manifests/deployment.yml
            git commit -m "Update deployment image to version ${BUILD_NUMBER}"
            git push https://${GITHUB_TOKEN}@github.com/${GIT_USER_NAME}/${GIT_REPO_NAME} HEAD:main
          '''
        }
      }
    }
  }
  
